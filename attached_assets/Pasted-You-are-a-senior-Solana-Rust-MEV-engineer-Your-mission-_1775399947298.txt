You are a senior Solana/Rust MEV engineer.

Your mission: Upgrade an existing MEV bot to run live on Solana (Devnet or Mainnet) using real Jupiter APIs. Remove all mocks or simulated data. The bot must execute real transactions, read wallet balances correctly, and handle flash loans/arbitrage atomically.

────────────────────────────────────
1️⃣ HARD REQUIREMENTS
────────────────────────────────────
❌ No mock data  
❌ No simulated transactions  
❌ No placeholder routes  
❌ DO NOT rely on transactionSubscribe as the primary data source  

✅ Only live API calls  
✅ Executable on-chain transactions  
✅ Wallet balance reading must be correct (Devnet/Mainnet)  

⚠️ CRITICAL:
If transactionSubscribe is unavailable, unreliable, rate-limited, or unsupported:
→ You MUST find and implement a real alternative.
→ You MUST test it live and verify it works before integrating.

Accepted alternatives include (but are not limited to):
- logsSubscribe (WebSocket)
- blockSubscribe
- getSignaturesForAddress polling
- getTransaction polling
- Geyser plugins / indexers
- Third-party providers (Helius, QuickNode, Alchemy, Triton, etc.)

🚫 Do NOT stop at "not available"
✅ You must search, test, validate, and integrate a WORKING solution

────────────────────────────────────
2️⃣ Verified Live Jupiter APIs
────────────────────────────────────

Ultra Quote API – get route + transaction:
GET https://api.jup.ag/ultra/v1/order?inputMint=So11111111111111111111111111111111111111112&outputMint=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v&amount=1000000&taker=27mnFgBVzcP35qjk2aDbLaygKQx4nLZtbB44Nhggrdsf

Headers:
  accept: application/json
  x-api-key: 3a151309-ead7-4a6a-836f-4a142e1467ac

Token Price API:
GET https://api.jup.ag/price/v3?ids=So11111111111111111111111111111111111111112,EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v

Token Metadata API:
GET https://api.jup.ag/tokens/v2/search?query=SOL

✅ These endpoints are confirmed working. Replace ALL old endpoints.

────────────────────────────────────
3️⃣ Tasks for AI – Upgrade & Fix
────────────────────────────────────

🔹 Ultra Quote Integration
- Fetch route + outAmount + fees + priceImpact + transaction (Base64)
- Deserialize and sign transaction
- Execute directly on-chain
- Fallback to legacy ONLY if Ultra fails

🔹 Token Prices
- Fetch real-time prices
- Calculate profitability
- Reject unprofitable trades

🔹 Token Metadata
- Build symbol → mint mapping
- Handle decimals correctly

🔹 Wallet Balance Fix
- Fix zero-balance bug
- Validate:
  - RPC endpoint
  - keypair loading
  - token accounts (SPL)
- Ensure accurate balances across all tokens

🔹 Flash Loan + Swap Execution
- Combine Jupiter transaction + flash loan logic
- Validate:
  - fees
  - slippage
  - price impact
  - expected profit
- Execute atomically

🔹 Rust Refactor
- Remove ALL mocks / fake tx
- Create structs for:
  - Ultra API response
  - Price API
  - Token Metadata
- Implement:
fn get_best_route_and_transaction(...) -> Result<RouteExecutionData>

Returns:
- route
- expected output
- fees
- price impact
- serialized transaction

────────────────────────────────────
4️⃣ LIVE DATA PIPELINE (CRITICAL)
────────────────────────────────────

You MUST implement a REAL-TIME transaction monitoring system.

❌ Do NOT rely only on transactionSubscribe  
✅ You MUST implement a WORKING alternative

Steps:
1. Search for available Solana streaming methods
2. Test them live
3. Measure latency and reliability
4. Integrate the best one

Preferred priority:
1. logsSubscribe (fast + widely supported)
2. blockSubscribe
3. RPC polling (getSignaturesForAddress + getTransaction)
4. External indexers (Helius / Alchemy / etc.)

Bot MUST:
- Detect new opportunities in real-time
- React within milliseconds
- Not depend on a single failing method

────────────────────────────────────
5️⃣ Hot Loop / Live Trading
────────────────────────────────────

Loop:
- Fetch Ultra Quote
- Fetch Prices
- Monitor chain (via alternative to transactionSubscribe)
- Evaluate profit
- Execute ONLY if profitable

────────────────────────────────────
6️⃣ Transaction Validity
────────────────────────────────────

- Validate blockhash freshness
- Check lastValidBlockHeight
- Retry on failure
- Handle dropped transactions

────────────────────────────────────
7️⃣ Logging
────────────────────────────────────

[TRADE]
inputMint=
outputMint=
inAmount=
outAmount=
fees=
priceImpact=
expectedProfit=
walletBalance=
route=
txSignature=

────────────────────────────────────
8️⃣ FINAL OBJECTIVE
────────────────────────────────────

Deliver a MEV bot that:

✅ Uses Jupiter Ultra APIs ONLY  
✅ Executes REAL on-chain transactions  
✅ Reads wallet balances correctly  
✅ Detects REAL arbitrage opportunities  
✅ Runs continuously and reliably  
✅ Uses a WORKING alternative to transactionSubscribe  

🚫 Any mock, fake route, or broken data source MUST be removed